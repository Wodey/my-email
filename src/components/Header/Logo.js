import React from "react";
import styled from 'styled-components';

const Body = styled.div`
  font-size: 2rem;
  display: flex;
  padding-left: 30px;
`;

const Logo = () => (
  <Body>
    LeMail
  </Body>
);

export default Logo;
